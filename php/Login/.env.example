# Environment Configuration for Interwave
# Copy this file to .env and update the values

# Application Environment
APP_ENV=development
APP_DEBUG=true

# Database Configuration
DB_HOST=localhost
DB_NAME=user_auth_system
DB_USER=interwave
DB_PASS=WV$PsDekNQ23yseJJTaP

# JWT Configuration (IMPORTANT: Generate a new secret key!)
# Generate with: openssl rand -hex 32
JWT_SECRET=your_super_secure_random_key_here_change_this_immediately

# Security Settings
ENABLE_ERROR_DISPLAY=false
LOG_ERRORS=true
SESSION_TIMEOUT=7200

# Rate Limiting
MAX_LOGIN_ATTEMPTS=5
LOGIN_TIMEOUT=300

# Application Settings
SITE_NAME=Interwave
ADMIN_EMAIL=admin@interwave.com